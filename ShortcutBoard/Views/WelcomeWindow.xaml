<Window x:Class="ShortcutBoard.Views.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShortcutBoard へようこそ"
        Width="440" SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1" CornerRadius="10">
        <StackPanel Margin="24,22">
            <!-- ヘッダー -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,14">
                <Border Background="{StaticResource AccentBrush}" CornerRadius="6"
                        Width="28" Height="28" Margin="0,0,10,0">
                    <TextBlock Text="S" Foreground="White" FontWeight="Bold" FontSize="16"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="ShortcutBoardへようこそ！" FontSize="17" FontWeight="SemiBold"
                           VerticalAlignment="Center" />
            </StackPanel>

            <!-- 本文 -->
            <TextBlock Foreground="{StaticResource TextBrush}" FontSize="13.5"
                       TextWrapping="Wrap" LineHeight="22" LineStackingStrategy="BlockLineHeight">
                よく使うショートカットキーを、自分用にまとめておけるアプリです。<LineBreak/><LineBreak/>
                <Run Text="Ctrl + Shift + A" FontWeight="SemiBold" Foreground="#8FBEFF" /> でいつでも一覧を表示できます。<LineBreak/><LineBreak/>
                右上の「編集」から、項目の追加・編集・削除ができます。<LineBreak/>
                まずはサンプルを自分用に書き換えて使ってみてください。
            </TextBlock>

            <!-- ボタン -->
            <Button Content="はじめる" Style="{StaticResource AccentButton}"
                    HorizontalAlignment="Right" Margin="0,18,0,0" Padding="20,7"
                    MinWidth="110" Click="Start_Click" IsDefault="True" />
        </StackPanel>
    </Border>
</Window>
